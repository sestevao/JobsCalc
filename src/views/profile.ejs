<!DOCTYPE html>
<html lang="en-GB">
  <%- include('parts/head', {jsFile: "", cssFile: "profile"}) %>

  <body id="page-profile">
    <%- include('parts/page-header', {title: 'My Profile' }) %>

    <div class="container animate-up delay-2">
      <aside class="card">
        <img src="<%= profile.avatar %>" alt="<%= profile.name %>" />
        <h2><%= profile.name %></h2>
        <p>
          Your hourly value is <br />
          <strong>£ <%= profile["value-hour"].toFixed(2).replace(".",",") %></strong>
        </p>
        <button class="button green" form="form-profile" type="submit">
          Save data
        </button>
      </aside>

      <main>
        <form id="form-profile" method="POST" action="/profile">
          <fieldset>
            <legend>Profile data</legend>
            <div class="separator light"></div>

            <div class="input-group">
              <div class="input-wrapper">
                <label for="name">Name</label>
                <input
                  type="text"
                  id="name"
                  name="name"
                  value="<%= profile.name %>"
                />
              </div>

              <div class="input-wrapper">
                <label for="avatar">Photo link</label>
                <input
                  placeholder="https://"
                  type="url"
                  id="avatar"
                  name="avatar"
                  value="<%= profile.avatar %>"
                />
              </div>
            </div>
          </fieldset>

          <fieldset>
            <legend>Planning</legend>
            <div class="separator light"></div>

            <div class="input-group">
              <div class="input-wrapper">
                <label for="monthly-budget">How much do I want to earn per month?</label>
                <input type="amount" step="0.1" id="monthly-budget"
                name="monthly-budget" placeholder="£" value="<%=
                profile["monthly-budget"] %>" />
              </div>

              <div class="input-wrapper">
                <label for="hours-per-day">How many hours <br />
                  want to work a day?</label>
                <input type="number" id="hours-per-day" name="hours-per-day"
                value="<%= profile["hours-per-day"] %>" />
              </div>
            </div>

            <div class="input-group">
              <div class="input-wrapper">
                <label for="days-per-week">How many days <br />
                  do I want to work per week?</label>
                <input type="number" id="days-per-week" name="days-per-week"
                value="<%= profile["days-per-week"] %>" />
              </div>

              <div class="input-wrapper">
                <label for="vacation-per-year">How many weeks <br />
                  a year want to take a vacation?</label>
                <input type="number" id="vacation-per-year"
                name="vacation-per-year" value="<%= profile["vacation-per-year"]
                %>" />
              </div>
            </div>
          </fieldset>
        </form>
      </main>
    </div>
  </body>
</html>
