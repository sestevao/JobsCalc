<!DOCTYPE html>
<html lang="en-GB">
<%- include('parts/head', {jsFile: "job-edit", cssFile: "job"}) %>
    
<body id="page-job-edit">
    <%- include('parts/page-header', {title: 'Edit Job' }) %>

    <div class="container flex animate-up delay-2">
        <form id="form-job" method="POST" action="/job/<%= job.id %>">
            <fieldset>
                <legend>Job Data</legend>
                <div class="separator light"></div>

                <div class="input-wrapper">
                    <label for="name">Job name</label>
                    <input type="text" id="name" name="name" value="<%= job.name %>" />
                </div>

                <div class="input-group">
                    <div class="input-wrapper">
                        <label for="daily-hours">How many <br /> hours per day will you dedicate to the job?</label>
                        <input type="number" step="0.1" id="daily-hours" name="daily-hours" value="<%= job["daily-hours"] %>"/>
                    </div>

                    <div class="input-wrapper">
                        <label for="total-hours">Estimated <br /> hours for this Job?</label>
                        <input type="number" id="total-hours" name="total-hours" value="<%= job["total-hours"] %>"
                        />
                    </div>
                </div>
            </fieldset>
        </form>
        </main>

        <aside class="card">
            <img src="/images/money-color.svg" alt="money image">
            <p>The value of the project was in <strong>£ <%= job.budget.toFixed(2).replace('.',',') %></strong></p>

            <div class="button-group">
                <button class="button green" form="form-job" type="submit" title="Save">Save</button>
                <a href="#" class="button gray open-modal">
                    <img src="/images/trash-24.svg" alt="Cancel" title="Cancel">
                </a>
            </div>
        </aside>
    </div>
    <!-- end container -->

    <div class="modal-wrapper">
        <div class="modal">
            <img src="/images/trash-48.svg" alt="Excluir Job" title="Excluir Job" />

            <h3>Delete Job</h3>

            <p>Do you really want to delete this job? <br /> It will be erased forever.</p>

            <footer>
                <a class="button gray" href="#">Cancel</a>
                <button class="button red" type="submit" form="delete-job">Delete Job</button>
            </footer>
        </div>

        <form method="post" action="/job/delete/<%= job.id %>" id="delete-job"></form>
    </div>
</body>
</html>